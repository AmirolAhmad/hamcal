<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "HamCal", root_path, class: "navbar-brand" %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if user_signed_in? && current_user %>
          <li>
            <%#= link_to clients_path do %>
              <span class="glyphicon glyphicon-link"></span> Manage Client
            <%# end %>
          </li>
          <li>
            <%#= link_to products_path do %>
              <span class="glyphicon glyphicon-link"></span> Manage Product
            <%# end %>
          </li>
          <li>
            <%#= link_to orders_path do %>
              <span class="glyphicon glyphicon-link"></span> Manage Order
            <%# end %>
          </li>
        <% end %>
      </ul>
      <% if user_signed_in? %>
        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Welcome, <%= current_user.email %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to settings_path do %>
                  <span class="glyphicon glyphicon-user small" aria-hidden="true"></span> Account Settings
                <% end %>
              </li>
              <li role="separator" class="divider"></li>
              <li>
                <%= link_to destroy_user_session_path, method: :delete do %>
                  <span class="glyphicon glyphicon-log-out small" aria-hidden="true"></span> Logout
                <% end %>
              </li>
            </ul>
          </li>
        </ul>
      <% else %>
        <div class="btn-group navbar-btn navbar-right">
          <%= link_to login_path, class: "btn btn-default btn-sm" do %>
          <span class="glyphicon glyphicon-user" aria-hidden="true"></span> Sign In
          <% end %>
          <%= link_to register_path, class: "btn btn-primary btn-sm" do %>
            <span class="glyphicon glyphicon-lock" aria-hidden="true"></span> Create New Account
          <% end %>
        </div>
      <% end %>
    </div>

  </div>
</nav>
